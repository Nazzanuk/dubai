app.component('heroItem', {
    templateUrl: 'hero',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});