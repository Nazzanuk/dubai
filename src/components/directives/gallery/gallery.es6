app.component('galleryItem', {
    templateUrl: 'gallery.html',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});