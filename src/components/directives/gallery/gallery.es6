app.component('galleryItem', {
    templateUrl: 'gallery',
    restrict: 'EA',
    //restrict: 'ECA',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});