app.component('vacanciesItem', {
    templateUrl: 'vacancies.html',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});