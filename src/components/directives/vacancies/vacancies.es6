app.component('vacanciesItem', {
    templateUrl: 'vacancies',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});