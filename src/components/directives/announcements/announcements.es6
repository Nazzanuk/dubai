app.component('announcementsItem', {
    templateUrl: 'announcements',
    restrict: 'ECA',
    bindings: {},
    controller: function ($scope) {

        var init = () => {
        };

        init();

        _.extend($scope, {});
    }
});
