app.component('promosItem', {
    templateUrl: 'promos',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});