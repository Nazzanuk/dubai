app.component('promosItem', {
    templateUrl: 'promos',
    restrict: 'ECA',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});