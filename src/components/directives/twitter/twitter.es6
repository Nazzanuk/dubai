app.component('twitterItem', {
    templateUrl: 'twitter.html',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});