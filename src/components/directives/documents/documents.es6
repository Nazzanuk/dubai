app.component('documentsItem', {
    templateUrl: 'documents',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});