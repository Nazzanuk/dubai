app.component('documentsItem', {
    templateUrl: 'documents.html',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});