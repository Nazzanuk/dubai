app.component('pressReleasesItem', {
    templateUrl: 'press-releases',
    restrict: 'ECA',
    bindings: {},
    controller: function ($scope) {

        var init = () => {};

        init();

        _.extend($scope, {

        });
    }
});